{
  "buildCommand": "npm init -y > /dev/null && npm install @tailwindcss/cli && export PATH=\"$PWD/node_modules/.bin:$PATH\" && rustup target add wasm32-unknown-unknown && cargo install trunk --locked && trunk build --release",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/|_next/|static/|favicon.ico).*)", "destination": "/index.html" }
  ]
}
